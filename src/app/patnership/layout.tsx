import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Kemitraan - PinusPintar.id",
};

export default function PatnershipLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
